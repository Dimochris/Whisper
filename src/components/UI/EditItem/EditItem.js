import React from 'react';

const EditItem = (props) => {
    const type = props.type;
    const id = props.id;
    
    return (
        <div>
            
        </div>
    );
};

export default EditItem;